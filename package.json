{
  "name": "statidocs",
  "devDependencies": {
    "@tailwindcss/typography": "^0.5",
    "tailwindcss": "^3.4"
  },
  "scripts": {
    "build": "curl -sSOL https://cecil.app/build.sh && bash ./build.sh"
  },
  "engines" : { 
    "node" : ">=16.0.0 <20.0.0"
  }
}
