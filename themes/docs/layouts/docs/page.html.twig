{% extends '_default/page.html.twig' %}

    {%- block content ~%}
      {%- include 'partials/sidebar.html.twig' ~%}
      <article class="w-full lg:ml-64 xl:ml-80 overflow-x-hidden px-4 xl:px-16 py-8">
        <details class="mb-4 xl:hidden">
          <summary class="cursor-pointer text-slate-500 dark:text-slate-300">
            {% trans %}On this page{% endtrans ~%}
          </summary>
          {{ page.body|toc }}
        </details>
        {#- article content ~#}
        <section id="content" class="markdown dark:markdown-invert max-w-none">
          {%- block article ~%}
          {{ page.content }}
          {%- endblock article ~%}
        </section>
        <footer class="pt-6">
          {%- include 'partials/edit.html.twig' ~%}
        </footer>
        {#- prev/next navigation ~#}
        {%- if page.next is defined or page.prev is defined ~%}
        <nav class="mt-8">
          {%- if page.prev is defined ~%}
          <a class="float-left font-medium hover:no-underline" href="{{ url(page.prev) }}" title="{{ page.prev.title }}">
            <div class="mb-2 text-slate-500 dark:text-slate-400">{% trans %}Previous{% endtrans %}</div>
            <div class="hover:opacity-75">← {{ page.prev.title }}</div>
          </a>
          {%- endif ~%}
          {%- if page.next is defined ~%}
          <a class="float-right font-medium hover:no-underline" href="{{ url(page.next) }}" title="{{ page.next.title }}">
            <div class="mb-2 text-slate-500 dark:text-slate-400 text-right">{% trans %}Next{% endtrans %}</div>
            <div class="hover:opacity-75">{{ page.next.title }} →</div>
          </a>
          {%- endif ~%}
        </nav>
        {%- endif ~%}
      </article>
      {#- "On this page" navigation ~#}
      <div>
        <nav id="on-this-page" class="hidden xl:block py-6 w-64 text-slate-500 dark:text-slate-300 sticky top-[80px]">
          <h3 class="mb-2 font-semibold text-sm uppercase tracking-wider">{% trans %}On this page{% endtrans %}</h3>
          {{ page.body|toc }}
          {%- include 'partials/edit.html.twig' ~%}
        </nav>
      </div>
    {%- endblock content ~%}